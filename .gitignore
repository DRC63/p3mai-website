# Office lock/temp files
~$*

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Environment / secrets (never commit)
.env
.env.*
!.env.example

# Internal reading summaries / planning binaries — large, and derived from
# third-party published material. Kept locally, never published or committed.
reference/
